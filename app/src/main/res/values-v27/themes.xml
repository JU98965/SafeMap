<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.PaiMap" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">#282728</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- 네비게이션 바 색깔 바꿈, 단 색깔을 바꾸는 순간 아이콘 색은 흰색으로 고정됨 -->
        <item name="android:navigationBarColor">#282728</item>
        <!-- 네비게이션 바 아이콘 색깔 바꿈, api 27 이상 작동 ture(검은색) false(흰색) -->
        <item name="android:windowLightNavigationBar">false</item>

        <!-- 스테이터스 바 아이콘 색깔 바꿈, ture(검은색) false(흰색) -->
        <item name="android:windowLightStatusBar">false</item>
        <!-- Customize your theme here. -->
    </style>
</resources>